# 🔒 보안 가이드

## ⚠️ GitHub 업로드 전 필수 확인사항

### 1. 환경 변수 설정
GitHub에 업로드하기 전에 반드시 `.env` 파일을 생성하고 실제 API 키를 설정하세요:

```env
# 실제 API 키로 교체 필요
OPENAI_API_KEY=sk-your-actual-openai-api-key-here
ELEVENLABS_API_KEY=your-actual-elevenlabs-api-key-here

# 강력한 비밀번호로 변경 필요
SESSION_SECRET=your-very-strong-session-secret-here
TEACHER_PASSWORD=your-strong-teacher-password-here

# 데이터베이스 설정
MONGO_URI=mongodb://localhost:27017/speaking_app

# 서버 설정
PORT=3000
NODE_ENV=production

# 보안 설정
COOKIE_SECURE=true
TRUST_PROXY=true
```

### 2. 보안 체크리스트

#### ✅ 완료된 보안 수정사항:
- [x] `server.js`에서 하드코딩된 비밀번호 제거
- [x] `teacher.html`에서 하드코딩된 비밀번호 제거
- [x] 환경 변수를 통한 API 키 관리
- [x] `.gitignore`에 `.env` 파일 제외 설정

#### ⚠️ 추가로 확인해야 할 사항:
- [ ] 프로덕션 환경에서 강력한 비밀번호 사용
- [ ] HTTPS 사용 (프로덕션 환경)
- [ ] API 키 정기적 로테이션
- [ ] MongoDB 접근 권한 설정

### 3. TBLT 시스템 보안 고려사항

#### 과업 데이터 보안
- **세션 기반 접근**: 각 과업은 고유한 sessionId로 보호
- **교사 인증**: 과업 생성 및 관리 기능은 교사 인증 필요
- **데이터 암호화**: 민감한 과업 데이터는 암호화하여 저장

#### AI 스캐폴딩 보안
- **프롬프트 보호**: AI 스캐폴딩 프롬프트는 교사만 수정 가능
- **대화 기록**: 학생과의 대화는 세션별로 격리
- **API 사용량 제한**: OpenAI API 사용량 모니터링 및 제한

### 4. 프로덕션 배포 시 주의사항

1. **환경 변수**: 모든 민감한 정보를 환경 변수로 관리
2. **HTTPS**: SSL/TLS 인증서 사용
3. **방화벽**: 필요한 포트만 개방
4. **로그 관리**: 민감한 정보가 로그에 기록되지 않도록 주의
5. **정기 업데이트**: 의존성 패키지 정기적 업데이트

### 5. API 키 보안

- **OpenAI API 키**: 사용량 제한 설정 및 모니터링
- **ElevenLabs API 키**: 사용량 모니터링
- **정기적 로테이션**: 3-6개월마다 API 키 교체
- **접근 권한**: 최소 권한 원칙 적용

### 6. 데이터 보안

- **세션 데이터**: 암호화된 세션 저장
- **과업 데이터**: 교사가 생성한 과업 정보 보호
- **대화 데이터**: 학생 개인정보 최소 수집
- **로그 데이터**: 민감한 정보 제외
- **백업**: 암호화된 백업 저장

## 🚨 GitHub 업로드 전 최종 체크

1. `.env` 파일이 `.gitignore`에 포함되어 있는지 확인
2. 하드코딩된 비밀번호가 없는지 확인
3. API 키가 코드에 직접 포함되지 않았는지 확인
4. 테스트용 비밀번호가 프로덕션에 사용되지 않는지 확인
5. 과업 데이터에 민감한 정보가 포함되지 않았는지 확인

## 📞 문제 발생 시

보안 문제가 발견되면 즉시:
1. API 키 재발급
2. 비밀번호 변경
3. 관련 서비스 재시작
4. 로그 확인 및 정리
5. 과업 데이터 보안 검토